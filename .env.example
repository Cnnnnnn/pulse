# Pulse 本地开发环境变量示例文件。
# 复制为 `.env` 后填入真实值。`.env` 已被 .gitignore 忽略，不会进入版本库。

# ──────────────────────────────────────────────────────────────────────
# GitHub Personal Access Token — 解除未登录 60 次/小时 API 限流。
# 申请：https://github.com/settings/tokens （无需任何 scope，留空即可）。
# 也可在 Pulse「设置 → GitHub」面板内配置（优先于此处的环境变量）。
# GITHUB_TOKEN=

# ──────────────────────────────────────────────────────────────────────
# Artificial Analysis API Key — AI 榜单排名模块（客观分 / 价格 / 速度）。
# 官方 API：https://artificialanalysis.ai/insights (Free tier 100 次/天, Pro 500, Commercial 1000+)
# 使用方式：x-api-key 请求头，强制署名（页面脚注展示）。
# 留空则 AA 维度回退 GitHub 社区快照（仍可用），不阻断启动。
# ARTIFICIAL_ANALYSIS_API_KEY=

# ──────────────────────────────────────────────────────────────────────
# TMDB API Key — 电影模块 L3 兜底（内地 region=CN），以及香港/澳门主源。
# 官方申请：https://www.themoviedb.org/settings/api （免费，选 "Developer" 账号）。
# 留空则内地仍走猫眼；香港/澳门没有猫眼城市，会落到示例数据。
# TMDB_API_KEY=

# ──────────────────────────────────────────────────────────────────────
# 足球球员身价榜：无需 key。数据来自 dcaribou/transfermarkt-datasets 公开 R2 CDN
# （https://pub-e682421888d945d684bcae8890b0ec20.r2.dev/data/），GitHub Actions 自动更新。
