# Pulse 本地开发环境变量示例文件。
# 复制为 `.env` 后填入真实值。`.env` 已被 .gitignore 忽略，不会进入版本库。

# ──────────────────────────────────────────────────────────────────────
# GitHub Personal Access Token — 解除未登录 60 次/小时 API 限流。
# 申请：https://github.com/settings/tokens （无需任何 scope，留空即可）。
# 也可在 Pulse「设置 → GitHub」面板内配置（优先于此处的环境变量）。
# GITHUB_TOKEN=

# ──────────────────────────────────────────────────────────────────────
# IsThereAnyDeal API Key — 游戏优惠聚合：让 Xbox（Microsoft Store）变实时。
# 申请：https://isthereanydeal.com/apps/my/ 注册 App 后获取。
# 留空则 Xbox 走示例数据；PlayStation / Switch 仍走示例数据（架构既定）。
# ITAD_API_KEY=

# ──────────────────────────────────────────────────────────────────────
# Artificial Analysis API Key — AI 榜单排名模块（客观分 / 价格 / 速度）。
# 官方 Free API：https://artificialanalysis.ai/
# 使用方式：x-api-key 请求头，1000 次/天限流，强制署名（页面脚注展示）。
# 留空则 AA 维度自动回退 GitHub 社区快照（仍可用），不阻断启动。
# ARTIFICIAL_ANALYSIS_API_KEY=
