# v2.39.0 — A1 Changelog 摘要 + Q4 v2 启动优化

> 发版日期: 2026-06-24
> 主分支: main

## 新增

- **A1 Changelog 智能摘要**
  - Changelog 面板「✨ 3 件大事」按钮，按需调 LLM
  - 聚合当前 + 历史 changelog 文本，输出 `oneLiner` + 最多 3 条 `highlights`
  - 缓存 `state.json.changelog_summary_cache` (30 天 GC)
  - 新 prompt `changelog_summary` (Prompt 模板可配)

## 优化 (Q4 v2)

- **主进程**: 全文搜索索引 `buildFromState` 延后到 `setImmediate`，不阻塞 bootstrap
- **Renderer**: `getConfig` 后立即 `render`，mutes/缓存/AI 订阅等走 `requestIdleCallback` 延后
- 目标: 降低 `readyMs` / 首屏可交互时间 (诊断抽屉仍可看 `startup_samples`)

## 范围说明

- A1 需配置 AI API Key；点击才调 LLM，不后台预取
- Q4 v2 未做 bundle 拆 chunk (需 DevTools profile 再定)；若 `readyMs` 仍 >800ms 可跑 `scripts/q4v2-profile.sh`

## 验证

- [x] vitest: changelog-summary + prompt-registry (6 prompts)
- [ ] 手测: 展开 changelog → 点「3 件大事」→ 看摘要列表
- [ ] 手测: 诊断抽屉 `readyMs` 对比上一版
