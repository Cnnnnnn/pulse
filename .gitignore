# Dependencies
node_modules/
.pnp/
.pnp.js

# codegraph local cache (index db + daemon socket)
.codegraph/
.code-review-graph/

# Build output
dist/
renderer-dist/
out/
# build/ 根下的 .cjs + .plist (electron-builder afterPack hook 路径) 必入库
# build/ 内的其它东西 (.DS_Store, scratch 脚本等) 还是 ignore
build/*
!build/*.cjs
!build/*.plist

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo
*~

# Temp files
*.tmp
*.local
# Agent scratch scripts (linter / 单文件扫描 / 临时 helper, 不入库)
.lint-warn.cjs
.list.cjs
.one.cjs
# 交付物 / 给外包的包 (独立于代码库)
deliverables/

# Test coverage
coverage/
.nyc_output/

# Env
.env
.env.local
.env.*.local

# Cache
.cache/
.parcel-cache/
.eslintcache

# mavis / agent stuff (会话级, 不入库)
.mavis/scratchpad/
.mavis/queue/
.mavis/branch-state.json
.mavis/plans/
.opencode/

# Brainstorming-2 visual companion session (HTML mockups, server state)
.superpowers/

# Bench output (scripts/startup-bench.js 跑出来的)
tests/fixtures/startup-bench-*.json

# config migration 备份
config.json.bak


# App built artifacts
*.dmg
*.blockmap
app.asar

# Worktrees (isolated feature workspaces)
.worktrees/

.cursor/

# Local AI agent scratch (debug test, 临时记的笔记, 不入库)
.workbuddy/
tests/_scratch/

# Task 4 behavior fixtures are tracked source files.
# Ignore only runtime-generated probe output.
tests/_smoke/.runtime/
tests/typescript/fixtures/.runtime/

# Code analysis tool output (本地工具产物, 不入库)
fuck-u-code-report.md

# Playwright visual regression (P3) — baseline 入库, 临时失败产物忽略
test-results/
playwright-report/

# ZCode CLI 本地会话/计划 (不入库)
.zcode/

# esbuild 抽出来的 CSS 在 renderer-dist/ 下面, 根目录 index.css 是 4bce47e
# 误提交的旧 build 产物 (现在引用都在 renderer-dist/), 防止再次误加
/index.css

.omm/*
docs/*
# Superpowers local execution ledger and reports
.superpowers/
