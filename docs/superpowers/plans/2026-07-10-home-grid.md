# Pulse Home Grid + 上次 nav 落点 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 Pulse 主窗口首屏改成一个"桌面式"grid，平铺 8 个顶级 nav；启动时若没有上次落点历史显示 grid，否则直接进入上次停留的 nav；任意模块都可一键回 grid。

**Architecture:** 把 `home` 加入 `activeNav` 的合法取值（`NAV_KEYS`），新增 `PERSISTABLE_NAV_KEYS` 白名单；`setActiveNav` 在白名单内的 key 切完后 fire-and-forget 落盘到主进程 `last_active_nav`；`bootstrap()` 在 `render()` 前 await `getLastActiveNav()`，有合法历史 → `setActiveNav` 一次定住 activeNav（避免首帧闪 grid）。

**Tech Stack:** Electron 主进程 (CommonJS) + Preact renderer (JSX) + @preact/signals + vitest + happy-dom

## Global Constraints

- Pulse 是 macOS 菜单栏 Electron App，主窗口默认 ≈ 360-400px 宽（popover）。
- 项目技术栈 + 版本：`package.json` 是 single source of truth（**不**改 Node / electron / preact 主版本）。
- 持久化单点：`state.json` 在 `app.getPath('userData')/state.json`（`state-store.js` 自动写）。
- 持久化沿用 `active_category` 范本（schema → load/save 函数 → IPC handler → preload 暴露）。
- 设计令牌：`docs/ui-design-system.md` 是真源，所有新样式用 `--gray-*`、`--blue-*` 等 token；**不写死魔数**。
- 颜色 / 间距 / 圆角 → 必须来自 `src/renderer/theme/tokens.css`（仓库已有）。
- a11y：focus 环可见（`var(--focus-ring)`）；触控目标 ≥ 44px；tile 是 `<button>`，Tab + Enter 可达。
- 不引入新依赖；不引入 hash 路由；不破坏现有 SideNav `setActiveNav` 调用点。
- 测试命令：`npx vitest run tests/path/file.test.js`；watch 模式用 `npx vitest watch`。

---

## File Structure

**新建**：
- `src/renderer/components/HomeGrid.jsx` — Home 首屏 grid 组件
- `src/renderer/components/HomeGrid.css` — HomeGrid 样式（与组件同级；不在全局 styles.css 灌入）
- `tests/main/state-store.lastActiveNav.test.js` — 主进程 `loadLastActiveNav` / `saveLastActiveNav` 单测
- `tests/renderer/worldcup/navStore.lastActiveNav.test.js` — `setActiveNav` 落盘逻辑单测

**修改**：
- `src/main/state-store-schema.js` — 加 `last_active_nav` 字段
- `src/main/state-store.js` — 加 `loadLastActiveNav` / `saveLastActiveNav`；导出；`PRESERVE_FIELDS` 加一项
- `src/main/ipc/register-core.js` — 注册 2 个 IPC handler
- `preload.js` — 暴露 `getLastActiveNav` / `saveLastActiveNav`
- `src/renderer/worldcup/navStore.js` — `NAV_KEYS` 加 `"home"`；新增 `PERSISTABLE_NAV_KEYS`；`activeNav` 改默认 `"home"`；`setActiveNav` 接入落盘
- `src/renderer/components/AppShell.jsx` — `activeNav === 'home'` 时渲染 `<HomeGrid />`
- `src/renderer/components/SideNav.jsx` — 顶部加 🏠 按钮（仅图标，折叠时也显示）
- `src/renderer/index.jsx` — `bootstrap()` 在 `render()` 之前 await `getLastActiveNav` + `setActiveNav`

---

### Task 1: 主进程持久化 `last_active_nav` 字段 + 纯函数

**Files:**
- Modify: `src/main/state-store-schema.js:40` (末尾追加一行)
- Modify: `src/main/state-store.js` (load/save 函数放 `loadActiveCategory` 紧邻位置；PRESERVE_FIELDS 末尾追加)
- Test: `tests/main/state-store.lastActiveNav.test.js`

**Interfaces:**
- Consumes: `load(statePath)` (已有 helper) / `patchState((next) => ..., statePath)` (已有)
- Produces:
  - `loadLastActiveNav(statePath?) => string | null` — 非法 / 缺失 → `null`
  - `saveLastActiveNav(key, statePath?) => state` — `key` 非法抛 `TypeError`；写盘 → atomic write → 保留所有其它字段

- [ ] **Step 1: 写失败测试**

新建 `tests/main/state-store.lastActiveNav.test.js`：

```javascript
/**
 * tests/main/state-store.lastActiveNav.test.js
 *
 * P-N HomeGrid 落点 — state-store load/save 纯函数测试.
 * 跑: npx vitest run tests/main/state-store.lastActiveNav.test.js
 */
import { describe, it, expect, beforeEach } from 'vitest';
import { mkdtempSync, rmSync } from 'fs';
import { tmpdir } from 'os';
import { join } from 'path';

let tmpDir;
let statePath;

beforeEach(() => {
  tmpDir = mkdtempSync(join(tmpdir(), 'pulse-nav-test-'));
  statePath = join(tmpDir, 'state.json');
});

const cleanup = () => { try { rmSync(tmpDir, { recursive: true, force: true }); } catch {} };

describe('loadLastActiveNav', () => {
  it('缺字段 / 状态文件不存在 → 返回 null', async () => {
    const { loadLastActiveNav } = await import('../../src/main/state-store.js');
    expect(loadLastActiveNav(statePath)).toBeNull();
    cleanup();
  });

  it('字段值 "home" → 返回 null (不污染)', async () => {
    const fs = await import('fs');
    fs.writeFileSync(statePath, JSON.stringify({ v: 1, apps: {}, last_active_nav: 'home' }));
    const { loadLastActiveNav } = await import('../../src/main/state-store.js');
    expect(loadLastActiveNav(statePath)).toBeNull();
    cleanup();
  });

  it('字段值合法 (e.g. "funds") → 返回该值', async () => {
    const fs = await import('fs');
    fs.writeFileSync(statePath, JSON.stringify({ v: 1, apps: {}, last_active_nav: 'funds' }));
    const { loadLastActiveNav } = await import('../../src/main/state-store.js');
    expect(loadLastActiveNav(statePath)).toBe('funds');
    cleanup();
  });

  it('字段值未知 key → 返回 null', async () => {
    const fs = await import('fs');
    fs.writeFileSync(statePath, JSON.stringify({ v: 1, apps: {}, last_active_nav: 'something-bogus' }));
    const { loadLastActiveNav } = await import('../../src/main/state-store.js');
    expect(loadLastActiveNav(statePath)).toBeNull();
    cleanup();
  });
});

describe('saveLastActiveNav', () => {
  it('非法 key 抛 TypeError', async () => {
    const { saveLastActiveNav } = await import('../../src/main/state-store.js');
    expect(() => saveLastActiveNav('home', statePath)).toThrow(TypeError);
    expect(() => saveLastActiveNav('', statePath)).toThrow(TypeError);
    expect(() => saveLastActiveNav(null, statePath)).toThrow(TypeError);
    cleanup();
  });

  it('合法 key 写盘 → reload 可读到', async () => {
    const { saveLastActiveNav, loadLastActiveNav } = await import('../../src/main/state-store.js');
    saveLastActiveNav('metals', statePath);
    expect(loadLastActiveNav(statePath)).toBe('metals');
    cleanup();
  });

  it('写 last_active_nav 不影响 active_category 等其它字段', async () => {
    const fs = await import('fs');
    fs.writeFileSync(statePath, JSON.stringify({
      v: 1, apps: {}, active_category: 'ai', last_opened: { foo: 1 },
    }));
    const { saveLastActiveNav, loadLastActiveNav } = await import('../../src/main/state-store.js');
    saveLastActiveNav('worldcup', statePath);
    const reloaded = JSON.parse(fs.readFileSync(statePath, 'utf8'));
    expect(reloaded.active_category).toBe('ai');
    expect(reloaded.last_opened).toEqual({ foo: 1 });
    expect(reloaded.last_active_nav).toBe('worldcup');
    cleanup();
  });
});
```

- [ ] **Step 2: 跑测试确认 FAIL**

Run: `npx vitest run tests/main/state-store.lastActiveNav.test.js`
Expected: 全部 FAIL（`loadLastActiveNav` / `saveLastActiveNav` is not a function in state-store.js）

- [ ] **Step 3: schema 加 `last_active_nav`**

Modify `src/main/state-store-schema.js:21` 行附近，在 `active_category` 后追加：

```javascript
  last_active_nav: { kind: 'string' },  // P-N: HomeGrid 落点 ('funds'|'metals'|'worldcup'|...)
```

- [ ] **Step 4: state-store.js 加 PRESERVE_FIELDS 项**

在 `src/main/state-store.js:255` 附近，`{ key: "ai_prompts", ... }` 后追加：

```javascript
  { key: "last_active_nav", kind: "string" },  // P-N: HomeGrid 落点
```

- [ ] **Step 5: state-store.js 加 load/save 纯函数**

紧邻 `loadActiveCategory` / `saveActiveCategory`（约 1066-1099 行）下方加：

```javascript
const PERSISTABLE_NAV_VALUES = new Set([
  'ithome', 'wechat-hot', 'worldcup', 'funds',
  'metals', 'stocks', 'ai-usage', 'versions',
]);

/**
 * P-N HomeGrid 落点: 读 last_active_nav.
 * 缺字段 / 非法值 → null (区别于 active_category 'all' 兜底,
 * renderer 需要明确"无历史"信号以决定是否显示首页).
 *
 * @param {string} [statePath]
 * @returns {string|null}
 */
function loadLastActiveNav(statePath = defaultPath()) {
  const s = load(statePath);
  if (!s) return null;
  const v = s.last_active_nav;
  if (typeof v !== 'string' || !PERSISTABLE_NAV_VALUES.has(v)) return null;
  return v;
}

/**
 * P-N HomeGrid 落点: 写 last_active_nav. atomic write,
 * 自动 preserve apps / mutes / last_opened / active_category / ...
 *
 * @param {string} key             'ithome' | 'wechat-hot' | 'worldcup' | 'funds' | 'metals' | 'stocks' | 'ai-usage' | 'versions'
 * @param {string} [statePath]
 * @returns {object} 写完后的完整 state
 * @throws {TypeError} 非合法 key
 */
function saveLastActiveNav(key, statePath = defaultPath()) {
  if (typeof key !== 'string' || !PERSISTABLE_NAV_VALUES.has(key)) {
    throw new TypeError('saveLastActiveNav: key must be a persistable nav key');
  }
  return patchState((next) => {
    next.last_active_nav = key;
  }, statePath);
}
```

- [ ] **Step 6: 在 module.exports 末尾加导出**

在 file 末尾 `};` 之前追加（参考 `loadActiveCategory` / `saveActiveCategory` 已暴露的位置）：

```javascript
  // P-N: HomeGrid 落点 (active_nav)
  loadLastActiveNav,
  saveLastActiveNav,
```

- [ ] **Step 7: 跑测试确认 PASS**

Run: `npx vitest run tests/main/state-store.lastActiveNav.test.js`
Expected: 7/7 PASS

- [ ] **Step 8: Commit**

```bash
git add src/main/state-store-schema.js src/main/state-store.js tests/main/state-store.lastActiveNav.test.js
git commit -m "feat(state-store): 持久化 last_active_nav — HomeGrid 落点记忆"
```

---

### Task 2: 主进程 IPC handler

**Files:**
- Modify: `src/main/ipc/register-core.js:349` (在 `save-active-category` 后插入)

**Interfaces:**
- Consumes: `ctx.safeHandle`, `stateStore.loadLastActiveNav()`, `stateStore.saveLastActiveNav(key)`
- Produces IPC:
  - `get-last-active-nav` → `{ lastActiveNav: string | null }`
  - `save-last-active-nav` → `{ ok: boolean, lastActiveNav?: string, reason?: string }`
    - 非法 key → `{ ok: false, reason: 'invalid_key' }`，**不抛**

- [ ] **Step 1: 添加 `get-last-active-nav` handler**

在 `register-core.js` line 349 后插入：

```javascript
  // P-N: HomeGrid 落点
  ipcMain.handle("get-last-active-nav", () => {
    try {
      return { lastActiveNav: stateStore.loadLastActiveNav() };
    } catch (err) {
      mainLog.warn("[ipc] get-last-active-nav threw", {
        msg: err && err.message,
      });
      return { lastActiveNav: null };
    }
  });
```

- [ ] **Step 2: 添加 `save-last-active-nav` handler (用 safeHandle)**

紧邻上一个 handler 后插入：

```javascript
  safeHandle(
    "save-last-active-nav",
    (_event, key) => {
      if (typeof key !== "string" || key.length === 0) {
        return {
          ok: false,
          reason: "invalid_key",
          lastActiveNav: stateStore.loadLastActiveNav(),
        };
      }
      try {
        const next = stateStore.saveLastActiveNav(key);
        return { ok: true, lastActiveNav: next.last_active_nav };
      } catch (err) {
        if (err && err.name === "TypeError") {
          return {
            ok: false,
            reason: "invalid_key",
            lastActiveNav: stateStore.loadLastActiveNav(),
          };
        }
        throw err;
      }
    },
    {
      logMeta: (_evt, key) => ({ key }),
      onError: () => ({
        ok: false,
        reason: "threw",
        lastActiveNav: stateStore.loadLastActiveNav(),
      }),
    },
  );
```

- [ ] **Step 3: Commit**

```bash
git add src/main/ipc/register-core.js
git commit -m "feat(ipc): HomeGrid 落点 — get-last-active-nav / save-last-active-nav"
```

---

### Task 3: preload 暴露

**Files:**
- Modify: `preload.js:51` (在 `saveActiveCategory` 暴露后追加)

- [ ] **Step 1: 在 contextBridge api 加 2 行**

在 `preload.js` `saveActiveCategory: (id) => ipcRenderer.invoke("save-active-category", id),` 后追加：

```javascript
  // P-N: HomeGrid 落点
  getLastActiveNav: () => ipcRenderer.invoke("get-last-active-nav"),
  saveLastActiveNav: (key) => ipcRenderer.invoke("save-last-active-nav", key),
```

- [ ] **Step 2: 跑现有 preload 测试确认未破坏**

Run: `npx vitest run tests/preload-platform.test.js`
Expected: PASS（如有别的 preload 测试覆盖，扫一遍 `tests/**/preload*`）

- [ ] **Step 3: Commit**

```bash
git add preload.js
git commit -m "feat(preload): 暴露 getLastActiveNav / saveLastActiveNav"
```

---

### Task 4: navStore 扩展白名单 + 默认值 + 落盘

**Files:**
- Modify: `src/renderer/worldcup/navStore.js` (`NAV_KEYS` / 新增 `PERSISTABLE_NAV_KEYS` / `activeNav` 默认 / `setActiveNav` 落盘)

**Interfaces:**
- Consumes: `import { api } from '../api.js'` (新增)
- Produces:
  - `PERSISTABLE_NAV_KEYS: Set<string>` —— 8 个顶级 nav (不包含 `"home"`)
  - `setActiveNav(key)` 行为扩展：合法 nav key 切完后 fire-and-forget `api.saveLastActiveNav(key)`

- [ ] **Step 1: 写失败测试**

新建 `tests/renderer/worldcup/navStore.lastActiveNav.test.js`：

```javascript
/**
 * tests/renderer/worldcup/navStore.lastActiveNav.test.js
 *
 * P-N HomeGrid 落点 — setActiveNav 落盘行为. mock api.
 * 跑: npx vitest run tests/renderer/worldcup/navStore.lastActiveNav.test.js
 */
import { describe, it, expect, beforeEach, vi } from 'vitest';

const saveCalls = [];
vi.mock('../../src/renderer/api.js', () => ({
  api: {
    saveLastActiveNav: (key) => {
      saveCalls.push(key);
      return Promise.resolve();
    },
  },
}));

describe('setActiveNav 落盘白名单', () => {
  beforeEach(async () => {
    saveCalls.length = 0;
    // 动态 import 确保 vi.mock 先注入
    const navStore = await import('../../src/renderer/worldcup/navStore.js');
    // 重置 activeNav 到一个不影响副作用的 nav
    navStore.setActiveNav('versions');
    saveCalls.length = 0;
  });

  it('setActiveNav("home") 不调 saveLastActiveNav (home 是显示态)', async () => {
    const { setActiveNav } = await import('../../src/renderer/worldcup/navStore.js');
    setActiveNav('home');
    // 给 microtask 一点机会
    await Promise.resolve();
    await Promise.resolve();
    expect(saveCalls).toEqual([]);
  });

  it('setActiveNav("funds") 调 saveLastActiveNav("funds")', async () => {
    const { setActiveNav } = await import('../../src/renderer/worldcup/navStore.js');
    setActiveNav('funds');
    await Promise.resolve();
    await Promise.resolve();
    expect(saveCalls).toEqual(['funds']);
  });

  it('setActiveNav("metals") 调 saveLastActiveNav("metals")', async () => {
    const { setActiveNav } = await import('../../src/renderer/worldcup/navStore.js');
    setActiveNav('metals');
    await Promise.resolve();
    await Promise.resolve();
    expect(saveCalls).toEqual(['metals']);
  });

  it('saveLastActiveNav reject 不影响 activeNav (fire-and-forget 语义)', async () => {
    const failingApi = await import('../../src/renderer/api.js');
    failingApi.api.saveLastActiveNav = vi.fn(() => Promise.reject(new Error('disk full')));
    const { setActiveNav, activeNav } = await import('../../src/renderer/worldcup/navStore.js');
    setActiveNav('ai-usage');
    await Promise.resolve();
    await Promise.resolve();
    expect(activeNav.value).toBe('ai-usage'); // 仍然切换成功
  });
});
```

- [ ] **Step 2: 跑测试确认 FAIL**

Run: `npx vitest run tests/renderer/worldcup/navStore.lastActiveNav.test.js`
Expected: FAIL（setActiveNav 还没接 api.saveLastActiveNav 时，case 2/3 也会 FAIL——预期）

- [ ] **Step 3: 修改 navStore.js**

`src/renderer/worldcup/navStore.js` 顶部 import 区（在 `import { clearWechatHotUnreadBadge }` 之后）新增：

```javascript
import { api } from "../api.js";
```

`NAV_KEYS` 集合（line 26）改为：

```javascript
const NAV_KEYS = new Set([
  "home",            // P-N: Home 首屏 (grid)
  "ithome",
  "wechat-hot",
  "worldcup",
  "funds",
  "metals",
  "stocks",
  "ai-usage",
  "versions",
]);

// P-N: HomeGrid 落点白名单 — "home" 是显示态, 不落盘.
// 跟 NAV_KEYS 的区别: NAV_KEYS 是 activeNav 全部合法值, 这里只挑出可持久化的 8 顶级 nav.
export const PERSISTABLE_NAV_KEYS = new Set([
  "ithome", "wechat-hot", "worldcup", "funds",
  "metals", "stocks", "ai-usage", "versions",
]);
```

`activeNav` 默认值（line 23）：

```javascript
// P-N: 无历史 → 显示 HomeGrid. bootstrap 拿到上次落点后会在 render 前覆盖.
export const activeNav = signal("home");
```

`setActiveNav` 函数（line 119）改为：

```javascript
export function setActiveNav(key) {
  if (!NAV_KEYS.has(key)) return;
  const prev = activeNav.value;
  activeNav.value = key;
  if (key === "funds" && prev !== "funds") {
    trackFundView();
    clearFundNavBadge();
  }
  if (key === "ai-usage" && prev !== "ai-usage") {
    clearAiUsageNavBadge();
  }
  if (key === "ithome" && prev !== "ithome") {
    trackIthomeView();
  }
  if (key === "wechat-hot" && prev !== "wechat-hot") {
    clearWechatHotUnreadBadge();
  }
  // P-N HomeGrid 落点: 仅持久化 8 顶级 nav, "home" 不写盘.
  // ponytail: 同步路径做过白名单过滤, home 是显示态, 不写盘.
  // 写盘失败仅 console.warn, 不阻断 UI.
  if (key !== "home" && PERSISTABLE_NAV_KEYS.has(key)) {
    if (typeof api?.saveLastActiveNav === "function") {
      api.saveLastActiveNav(key).catch(() => { /* noop */ });
    }
  }
}
```

- [ ] **Step 4: 跑测试确认 PASS**

Run: `npx vitest run tests/renderer/worldcup/navStore.lastActiveNav.test.js`
Expected: 4/4 PASS

- [ ] **Step 5: 跑现有相关测试确认未破坏**

Run: `npx vitest run tests/renderer/nav-refresh.test.js tests/renderer/worldcup-refresh-scores-loading.test.js tests/renderer/worldcup-betsStore.test.js`
Expected: 全部 PASS（`NAV_KEYS` + `setActiveNav` 是这些测试的关键路径）

- [ ] **Step 6: Commit**

```bash
git add src/renderer/worldcup/navStore.js tests/renderer/worldcup/navStore.lastActiveNav.test.js
git commit -m "feat(navStore): Home Grid 落点记忆 — 白名单 + 落盘 + 默认 home"
```

---

### Task 5: HomeGrid 组件 + CSS

**Files:**
- Create: `src/renderer/components/HomeGrid.jsx`
- Create: `src/renderer/components/HomeGrid.css`

**Interfaces:**
- Consumes: `setActiveNav` from `../worldcup/navStore.js`; Preact + signals 不需要 state
- Produces: `<HomeGrid />` 渲染 8 个 tile 的 grid，tile 是 `<button>`，点击调 `setActiveNav(tile.key)`

- [ ] **Step 1: 创建 HomeGrid.css**

新建 `src/renderer/components/HomeGrid.css`：

```css
/**
 * src/renderer/components/HomeGrid.css
 *
 * HomeGrid 自适应 grid + tile 视觉. 全部 token 取自 tokens.css.
 */
.home-grid {
  display: grid;
  gap: 12px;
  padding: 16px;
  grid-template-columns: repeat(2, 1fr); /* 默认紧凑 (popover 小窗) */
}

@media (min-width: 600px) {
  .home-grid {
    grid-template-columns: repeat(4, 1fr); /* 大屏 4 列 */
  }
}

.home-grid-tile {
  min-height: 88px;
  border-radius: var(--radius-md, 10px);
  background: var(--surface-elevated, rgba(255, 255, 255, 0.05));
  border: 1px solid transparent;
  padding: 14px;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  justify-content: space-between;
  text-align: left;
  cursor: pointer;
  font: inherit;
  color: var(--text-primary);
  transition: background-color 120ms ease, transform 120ms ease;
}

.home-grid-tile:hover,
.home-grid-tile:focus-visible {
  background: var(--surface-overlay, rgba(255, 255, 255, 0.10));
  transform: scale(1.02);
  outline: none;
}

.home-grid-tile:focus-visible {
  box-shadow: 0 0 0 3px var(--focus-ring, rgba(10, 132, 255, 0.55));
  border-color: var(--accent-primary, #007aff);
}

.home-grid-tile-icon {
  font-size: 28px;
  line-height: 1;
}

.home-grid-tile-title {
  font-size: 14px;
  font-weight: 600;
  margin-top: 8px;
}

.home-grid-tile-subtitle {
  font-size: 11px;
  color: var(--text-secondary);
  margin-top: 2px;
}
```

(注：CSS 中 `var(--xxx, fallback)` 的 fallback 是 Ponytail 风格的"safe default"——如果 design tokens 还没注入到具体主题，至少不破相。)

- [ ] **Step 2: 创建 HomeGrid.jsx**

新建 `src/renderer/components/HomeGrid.jsx`：

```jsx
/**
 * src/renderer/components/HomeGrid.jsx
 *
 * P-N HomeGrid — 首屏桌面式 grid, 8 个顶级 nav 平铺.
 * 不引用 NAV_ITEMS (避免双向耦合); HOME_TILES 独立维护.
 */
import { setActiveNav } from "../worldcup/navStore.js";
import "./HomeGrid.css";

// ponytail: emoji 兜底图标. 不画 8 个新 SVG, 不加 icon 库依赖.
// 后续要替换成 SVG 直接改这一处 map.
const ICON_EMOJI = {
  'ithome': '📰',
  'wechat-hot': '🔥',
  'worldcup': '⚽',
  'funds': '💼',
  'metals': '🏅',
  'stocks': '📈',
  'ai-usage': '🤖',
  'versions': '🔄',
};

const HOME_TILES = [
  { key: 'ithome',     title: 'IT 新闻',  subtitle: 'IT之家资讯 + AI 摘要',     },
  { key: 'wechat-hot', title: '微博热搜', subtitle: '微博实时热搜',              },
  { key: 'worldcup',   title: '世界杯',   subtitle: '2026 世界杯赛程',            },
  { key: 'funds',      title: '基金管理', subtitle: '基金持仓 + 实时盈亏',         },
  { key: 'metals',     title: '贵金属',   subtitle: '黄金白银实时 + 持仓',         },
  { key: 'stocks',     title: '选股',     subtitle: 'A股条件选股 + AI 分析',      },
  { key: 'ai-usage',   title: 'AI 用量',  subtitle: 'MiniMax coding plan 配额', },
  { key: 'versions',   title: '版本检查', subtitle: 'App 版本监控',              },
];

export function HomeGrid() {
  return (
    <div class="home-grid" role="grid" aria-label="Pulse 主菜单">
      {HOME_TILES.map((tile) => (
        <button
          key={tile.key}
          type="button"
          class="home-grid-tile"
          role="gridcell"
          aria-label={`进入 ${tile.title}`}
          onClick={() => setActiveNav(tile.key)}
        >
          <span class="home-grid-tile-icon" aria-hidden="true">
            {ICON_EMOJI[tile.key] ?? '•'}
          </span>
          <span class="home-grid-tile-title">{tile.title}</span>
          <span class="home-grid-tile-subtitle">{tile.subtitle}</span>
        </button>
      ))}
    </div>
  );
}

export default HomeGrid;
```

- [ ] **Step 3: 跑 build 确认 esbuild 不报错**

Run: `npm run build:renderer 2>&1 | tail -20`
Expected: 无 error，新 chunk 出现

- [ ] **Step 4: Commit**

```bash
git add src/renderer/components/HomeGrid.jsx src/renderer/components/HomeGrid.css
git commit -m "feat(renderer): HomeGrid 组件 — 8 顶级 nav 平铺首屏"
```

---

### Task 6: AppShell 接入 HomeGrid

**Files:**
- Modify: `src/renderer/components/AppShell.jsx:33-95`

- [ ] **Step 1: 在 AppShell 顶 import HomeGrid**

`src/renderer/components/AppShell.jsx` 顶部 import 区追加：

```javascript
import { HomeGrid } from './HomeGrid.jsx';
```

（在 `import { LazyNavPanel }` 之后即可。）

- [ ] **Step 2: 修改 return 部分**

`src/renderer/components/AppShell.jsx` line 86-94 的 `return (...)` 改为：

```jsx
  return (
    <div class={`app-shell${collapsed ? ' app-shell-collapsed' : ''}`}>
      <SideNav />
      <div class="app-shell-view">
        {nav === 'home'
          ? <HomeGrid />
          : <LazyNavPanel nav={nav} onCheck={onCheck} />}
      </div>
      <SearchModal />
    </div>
  );
```

- [ ] **Step 3: 跑 build 确认无错**

Run: `npm run build:renderer 2>&1 | tail -10`
Expected: 无 error

- [ ] **Step 4: Commit**

```bash
git add src/renderer/components/AppShell.jsx
git commit -m "feat(appshell): activeNav === 'home' 时渲染 HomeGrid"
```

---

### Task 7: SideNav 顶部加 🏠 按钮

**Files:**
- Modify: `src/renderer/components/SideNav.jsx`

**Interfaces:**
- Consumes: `setActiveNav` (已经 import 在 line 32)
- Produces: SideNav 顶部新增一个 `<button>` 类 `icon-btn`，`onClick={() => setActiveNav("home")}`，跟现有 `IconSettings` button 风格一致
- **位置决策 (用户拍定 2026-07-10)：** 在 **汉堡折叠按钮 (`IconMenu`) 之后、`IconSettings` 按钮之前** —— 跟现有用户进入 "修改侧导航" 同区域

- [ ] **Step 1: 在 SideNav 顶部按钮区加 🏠**

打开 `src/renderer/components/SideNav.jsx`，找到现有 `IconMenu`（汉堡折叠）按钮的 `<button>` JSX，紧跟其后、`IconSettings` 按钮之前的位置插入：

```jsx
<button
  type="button"
  class="sidenav-icon-btn"
  onClick={() => setActiveNav('home')}
  title="首页"
  aria-label="首页"
>
  🏠
</button>
```

注：emoji `🏠` 占位。用户授权"图标细节由 implementer 读源码决定" —— implementer 在这一步可以：① 复用仓库里现成 `IconHome` SVG 组件（替换 emoji 文本）；② 沿用 emoji 占位以保持 HomeGrid 视觉一致。最终方案以跟 SideNav 其它顶部按钮风格一致为准，并在 report 里说明选择。

- [ ] **Step 2: 跑 build 确认无错**

Run: `npm run build:renderer 2>&1 | tail -10`
Expected: 无 error

- [ ] **Step 3: Commit**

```bash
git add src/renderer/components/SideNav.jsx
git commit -m "feat(sidenav): 顶部加 🏠 按钮 — 任意模块一键回首页"
```

---

### Task 8: bootstrap 启动期加载 lastActiveNav

**Files:**
- Modify: `src/renderer/index.jsx:38` (import 区) + `bootstrap()` 函数 (line 242-266)

**Interfaces:**
- Consumes: `api.getLastActiveNav()` 已在 preload 暴露
- Produces: bootstrap 在 `render(<App />)` 之前 await `getLastActiveNav` + 若合法则 `setActiveNav`

- [ ] **Step 1: index.jsx 顶部 import `PERSISTABLE_NAV_KEYS`**

修改 `src/renderer/index.jsx:38` 附近的 import 区，从 `worldcup/navStore.js` 多 import 一个 symbol：

原：
```javascript
import { api } from './api.js';
```

之后追加：
```javascript
import { api } from './api.js';
import { setActiveNav, PERSISTABLE_NAV_KEYS } from './worldcup/navStore.js';
```

（注：`./worldcup/navStore.js` 在 index.jsx **已经**通过 `import('./worldcup/navStore.js')` 动态 import 过一次，为避免冲突改成顶层 static import。）

- [ ] **Step 2: 修改 bootstrap()**

`src/renderer/index.jsx` 的 `bootstrap()` 函数（line 242-266）改为：

```javascript
async function bootstrap() {
  applyPlatformBodyClass();
  initTheme();

  let cfg = { apps: [], check_on_launch: true };
  try {
    cfg = await api.getConfig();
    cfg.apps = cfg.apps || [];
  } catch (err) {
    log.error("getConfig failed:", err);
  }
  apps.value = cfg.apps;
  primeConfigCache(cfg);

  // P-N: HomeGrid 落点 — 拿到上次停留的 nav, 在 render 之前覆盖 activeNav,
  // 避免首帧闪 HomeGrid 再切到目标 (视觉撕裂).
  // 非法值 / 失败 → 静默, 留在默认 activeNav="home".
  if (typeof api.getLastActiveNav === 'function') {
    try {
      const { lastActiveNav } = await api.getLastActiveNav();
      if (lastActiveNav && PERSISTABLE_NAV_KEYS.has(lastActiveNav)) {
        setActiveNav(lastActiveNav);
      }
    } catch (err) {
      log.error("getLastActiveNav failed:", err);
    }
  }

  const mount = document.getElementById('app') || document.body;
  render(
    <ErrorBoundary>
      <App onCheck={triggerCheck} />
    </ErrorBoundary>,
    mount,
  );

  wireRendererListeners();
  scheduleDeferredBootstrap(cfg);
}
```

- [ ] **Step 3: 跑 build 确认无错**

Run: `npm run build:renderer 2>&1 | tail -10`
Expected: 无 error

- [ ] **Step 4: 手测启动路径**

```bash
npm run build:renderer
npm start
```

期望（手动验证，不写自动化）：
1. 首次启动（无 state.json 或 last_active_nav 缺失）→ 显示 HomeGrid
2. 点 HomeGrid 的"基金管理"tile → 进入基金管理 panel
3. 退出 App
4. 再次启动 → 直接进入基金管理 panel，**不显示 HomeGrid**
5. 点 SideNav 顶部 🏠 → 回到 HomeGrid
6. 在 HomeGrid 点 SideNav 的"贵金属" → 进入贵金属 panel
7. 退出 + 重启 → 直接进贵金属 panel

- [ ] **Step 5: Commit**

```bash
git add src/renderer/index.jsx
git commit -m "feat(bootstrap): 启动期 await lastActiveNav + 首帧不闪"
```

---

### Task 9: 端到端集成测试（vitest + happy-dom）

**Files:**
- Create: `tests/renderer/home-grid.integration.test.js`

**Interfaces:**
- Consumes: mock preload 的 `api.getLastActiveNav` / `api.saveLastActiveNav`，mock `worldcup/navStore.js` 的 signal 行为
- Produces: 验证 `bootstrap` 流程：拿到 `lastActiveNav` → `setActiveNav` 覆盖 activeNav.signal value

- [ ] **Step 1: 写集成测试**

新建 `tests/renderer/home-grid.integration.test.js`：

```javascript
// @vitest-environment happy-dom
/**
 * tests/renderer/home-grid.integration.test.js
 *
 * P-N HomeGrid 集成: bootstrap 路径在 happy-dom 下加载 lastActiveNav 后
 * activeNav 被正确覆盖. 实际预渲染我们直接观察 signal, 不需要把整个
 * Preact 树挂载进 happy-dom (复杂度过高, 与本测试目的不符).
 *
 * 跑: npx vitest run tests/renderer/home-grid.integration.test.js
 */
import { describe, it, expect, vi } from 'vitest';

describe('HomeGrid navStore 集成路径', () => {
  it('activeNav 默认值为 "home"', async () => {
    const { activeNav } = await import('../../src/renderer/worldcup/navStore.js');
    expect(activeNav.value).toBe('home');
  });

  it('setActiveNav("funds") 后 activeNav.value === "funds"', async () => {
    const { setActiveNav, activeNav } = await import('../../src/renderer/worldcup/navStore.js');
    setActiveNav('funds');
    expect(activeNav.value).toBe('funds');
  });

  it('setActiveNav("home") 后 activeNav.value === "home"', async () => {
    const { setActiveNav, activeNav } = await import('../../src/renderer/worldcup/navStore.js');
    setActiveNav('metals');
    setActiveNav('home');
    expect(activeNav.value).toBe('home');
  });

  it('PERSISTABLE_NAV_KEYS 不含 home', async () => {
    const { PERSISTABLE_NAV_KEYS } = await import('../../src/renderer/worldcup/navStore.js');
    expect(PERSISTABLE_NAV_KEYS.has('home')).toBe(false);
    expect(PERSISTABLE_NAV_KEYS.has('versions')).toBe(true);
    expect(PERSISTABLE_NAV_KEYS.size).toBe(8);
  });
});
```

- [ ] **Step 2: 跑测试确认 PASS**

Run: `npx vitest run tests/renderer/home-grid.integration.test.js`
Expected: 4/4 PASS

- [ ] **Step 3: Commit**

```bash
git add tests/renderer/home-grid.integration.test.js
git commit -m "test(renderer): HomeGrid 集成 + PERSISTABLE_NAV_KEYS 不变性"
```

---

### Task 10: 全量回归

**Files:** N/A — 验证

- [ ] **Step 1: 跑全量测试**

Run: `npm test`
Expected: 全部 PASS（包含本计划新增 + 现有所有）

- [ ] **Step 2: 跑 build**

Run: `npm run build`
Expected: 成功（macOS 上 mac-only 时可跳；如遇平台限制，至少 `npm run build:renderer` PASS）

- [ ] **Step 3: 手动 smoke test 全 8 个 nav**

启动 App，从 HomeGrid 进入每个 nav：

- ithome / wechat-hot / worldcup / funds / metals / stocks / ai-usage / versions

每次退出 + 重启，确认：
- 直接落进上次模块
- 不是第一次启动永远落 home

- [ ] **Step 4: 跑 lint / style check（如果有）**

Run: `npx vitest run --typecheck 2>&1 | tail -20`（vitest 默认不开，可跳过）

- [ ] **Step 5: 终 commit（如有未提交改动）**

```bash
git status
# 若有 staged/unstaged 改动:
git add -A
git commit -m "chore: home-grid 全量回归 — 收尾"
```

---

## Self-Review (ran inline after writing)

**Spec 覆盖检查** — spec 第 3-11 节每条要求都已落到 task：

| Spec 章节 | 落到 task |
|---|---|
| §3.1 activeNav 扩展 home + NAV_KEYS 加 home | T4-S3 |
| §3.4 SideNav 加 🏠 按钮 | T7 |
| §3.3 AppShell activeNav==='home' 走 HomeGrid | T6 |
| §4 HomeGrid 组件 | T5 |
| §5.1 PERSISTABLE_NAV_KEYS 白名单 | T4-S3 |
| §5.2 setActiveNav 落盘 | T4-S3 |
| §5.3 activeNav 默认 'home' | T4-S3 |
| §5.4 bootstrap await getLastActiveNav | T8 |
| §6.1 schema 加字段 | T1-S3 |
| §6.2 state-store load/save 纯函数 | T1-S5 |
| §6.3 PRESERVE_FIELDS（实现时发现的更准抽象） | T1-S4 |
| §6.4 IPC handler | T2 |
| §6.5 preload 暴露 | T3 |
| §7 错误处理 | 各 task 内 catch / TypeError |
| §8 数据流验证 | T9 (单元) + T10 (手测) |
| §9 测试（主进程 + renderer） | T1-S1 测试 + T4-S1 测试 + T9 集成 |

**Placeholder scan** — 全文无 TBD / TODO / "implement later"。

**类型一致性**：
- `loadLastActiveNav` → `string | null`（spec §6.2 + plan T1-S5 一致）
- `saveLastActiveNav(key)` 接收 8 nav key；`PERSISTABLE_NAV_KEYS` = 8 nav（不含 home）
- IPC `get-last-active-nav` 返回 `{ lastActiveNav }`，`save-last-active-nav` 返回 `{ ok, lastActiveNav?, reason? }`
- Preload `getLastActiveNav` / `saveLastActiveNav` 跟 IPC 名字一致
- `setActiveNav` 接收 `NAV_KEYS`（包含 home），落盘只用 `PERSISTABLE_NAV_KEYS`（不含 home）—— spec §5.2 跟 T4-S3 一致
