# 财经新闻详情页 UI 重设计提案

已完成对当前 `FinanceArticleView` 与 `finance.css` 的梳理，并产出重设计提案与视觉稿。

## 交付物

1. `deliverables/finance-article-detail-redesign-2026-07-29.md` — 完整 UI 改进方案
2. 桌面端视觉稿（内联 SVG）
3. 移动端视觉稿（内联 SVG）

## 主要改进方向

- 重构标题区：分类色大 chip + 26px 标题（桌面）+ 清晰 meta 行 + 圆角标签 pills
- 正文分段渲染：从 `pre-wrap` 一堵墙改为 `lead` + `<p>` 段落，行高 1.85，最大宽度 680px
- 新增顶部工具栏：返回 + 收藏 + 查看原文，右对齐，与正文区对齐
- 原文链接改为卡片：复用列表卡片设计语言（左侧分类色条、圆角）
- 相关推荐复用 `FinanceRow` 卡片网格，替代纯文字链接列表
- 移动端适配：标题 21px、正文 15px/1.8、相关推荐单列、触控热区保持 ≥44px

## 与列表页一致性

详情页复用列表页的分类色系统（`--cat-stock` 等）、卡片圆角与阴影、chip 与标签 pills，确保从列表点进详情时视觉无缝衔接。

## 后续步骤

按提案第 9 节优先级实施：先重构 DOM 与正文分段，再替换相关推荐为 FinanceRow，最后补移动端断点与原文卡片样式。
