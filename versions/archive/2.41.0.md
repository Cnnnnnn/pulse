# v2.41.0 — Q4 懒加载 + A4 可配置 + I6 SideNav 角标

> 发版日期: 2026-06-24
> 主分支: main

## 优化 (Q4 v4)

- **Renderer 懒加载**: 非默认 nav 面板 `dynamic import` + esbuild ESM splitting
- 首屏仅加载 `versions` 面板；`ithome` / `funds` / `ai-usage` 等按需拉 chunk
- `index.html` 改为 `<script type="module" src="renderer-dist/index.js">`

## 新增 (A4 v2)

- AI 用量异常阈值可在用量页 🔔 设置：开关、绝对阈值、相对倍数、重复提醒间隔
- IPC: `ai-usage:alert-prefs:get/set`，持久化 `state.json.ai_usage_alert_prefs`
- 页面 banner / 角标检测使用用户偏好（含 `lastNotified` 去重）

## 新增 (I6 v3)

- SideNav 角标扩展至 **基金**、**AI 用量**
- 基金盈亏提醒触发后 main → `sidenav:badge` → `fundUnreadBadge`
- AI 用量异常通知后同样推送角标；进入对应 tab 后清零

## 验证

- [x] vitest: anomaly-detect + sidenav fund/ai badge + renderer build
- [ ] 手测: 切换 nav 观察首屏加载与懒加载 chunk
- [ ] 手测: AI 用量 🔔 改阈值后 banner/通知行为
- [ ] 手测: 基金提醒后 SideNav funds 角标
