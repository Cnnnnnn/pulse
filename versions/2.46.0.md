# v2.46.0 — A1/A2 v2 打磨: few-shot 模板 + confidence 徽章

> 继 v2.45.0 loading / 错误中文化 / 缓存时间 后, 第二轮"用户能感知"打磨.
> 目标: 提升 A1/A2 模型质量稳定性 + 反馈密度.

## 改动

- **`src/ai/prompt-registry.js` 填 few-shot 默认示例**:
  - `upgrade_advice`: 2 条 (cold wait / hot upgrade 含安全修复)
  - `changelog_summary`: 1 条 (VSCode security + workspace trust + TS 性能)
  - 用户在 Settings → Prompt 仍可改/清空
- **A2 `UpgradeAdvice.jsx` 加 confidence 徽章**:
  - 3 档: high 绿 / medium 半透橙 / low 灰 (3px 圆点)
  - 位置: badge 旁, 不占 summary 横向空间
  - tooltip 提示 "模型置信度: high/medium/low"
- **A2 ↻ force 按钮 tooltip** 改为 "重新分析 (会消耗 AI 配额)"
- **`styles.css`**: `.upgrade-advice-confidence--{high,medium,low}` 颜色 + 透明度

## 测试

- `tests/main/prompt-registry.test.js` +2 case: A1/A2 默认 fewShot 非空 + resolvePrompt 返默认
- `tests/main/upgrade-advice.test.js` +1 case: buildAdviceMessages 注入 few-shot 示例
- `tests/main/changelog-summary.test.js` +1 case: buildSummaryMessages 注入 few-shot 示例
- `tests/renderer/upgrade-advice.test.jsx` +3 case: confidence=high/low 渲染 + ↻ tooltip
- **vitest 全量**: 2751 → 2758 (+7, 0 失败)

## 验证

- `npm run build:renderer` ✅
- esbuild 打包 OK, 改动均覆盖

## spec

- `docs/superpowers/specs/2026-06-24-a1a2v2-confidence-fewshot-design.md`

## 不做

- A1 confidence (后端无该字段)
- few-shot 编辑器 (Settings 现有 Prompt 编辑器已可改)
- A2 评分阈值化 (仍用 LLM 自由推荐)
